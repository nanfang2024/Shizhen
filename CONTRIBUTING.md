# 参与贡献

感谢你帮助改进拾帧。提交代码前，请先搜索现有 Issue，较大的平台适配或架构改动建议先创建功能讨论。

## 开发环境

- JDK 17
- Android SDK Platform 34
- Android Studio（需支持 Android Gradle Plugin 9.3.1）
- arm64-v8a Android 8.0+ 真机，用于媒体解析、下载和 FFmpeg 路径验证

```bash
./gradlew testDebugUnitTest lintDebug assembleDebug
```

## 提交要求

1. 保持解析器插件式结构，不要把平台专用规则塞入通用解析器。
2. 只处理匿名可访问的公开页面，不导入 Cookie、登录凭据、播放令牌或私人 API。
3. 不实现付费、DRM、登录或访问控制绕过。
4. 解析结果必须来自真实响应；不能用硬编码媒体假装成功。
5. 新增依赖时检查 Android/arm64-v8a 兼容性，并更新第三方许可证说明。
6. 不在日志、测试样例或提交历史中留下私人链接、Token、完整诊断报告或签名材料。
7. 平台规则可能随时变化。请为结构化提取逻辑增加最小、脱敏的单元测试。

## 问题报告

请使用仓库的 Issue 表单。解析、下载或转换问题应附上应用“历史记录 → 诊断中心”导出的脱敏 TXT；提交前仍需人工检查其中是否包含不希望公开的信息。
