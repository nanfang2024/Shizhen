# 已知限制

## 链接解析

- 平台网页结构和 yt-dlp 提取规则会变化；今天可解析的链接将来可能需要更新 `youtubedl-android` 或增加专用解析器。
- “已识别国内平台”不等于承诺所有资源均可提取。抖音、小红书、快手以及经 `v.douyin.com` 跳转到 `iesdouyin.com/xg/video/...` 的西瓜视频目前有当前作品级结构化解析器，网易云和 QQ 音乐有公开单曲解析通路，酷狗、酷我、咪咕只读取官方网页直接公开的音频；其他西瓜页面、哔哩哔哩、微博、优酷、爱奇艺、芒果 TV、知乎等主要依赖 yt-dlp；腾讯视频、好看视频、今日头条、梨视频、搜狐等没有可靠专用提取器时只走公开网页兜底。平台要求登录、验证码、会员、DRM、地区授权或只在 App 内通过私有接口播放时，拾帧会失败而不会绕过限制。
- 拾帧不接受 Cookie，不提供登录，不绕过地区、年龄、会员、付费、私密内容、DRM 或任何访问控制。HTTP 401/403、密码表单和常见登录提示会显示“该资源需要登录或存在访问限制，拾帧不支持提取。”
- 通用解析器读取服务器返回的 HTML 和 meta 标签，不运行页面 JavaScript。只在脚本执行后生成的媒体地址可能无法识别。
- 部分短链接可能跳转到应用唤起页、验证码页或风控页，因此无法解析。
- 西瓜视频经 `v.douyin.com` 分享时会先根据最终 `/xg/video/` 路径与作品 ID 纠正平台，再尝试四条官方、无需 Cookie 的公开页面路由。若这些官方页面同时返回 HTTP 500、404 或只返回 JavaScript 风控壳，拾帧不会使用登录 Cookie、私有签名接口或来源不明的代理服务继续提取；此时只能稍后重试或由平台恢复公开页面。
- Instagram 当前支持公开的 `p`、`reel`、`reels`、`tv` 和可由官方跳转展开的分享链接。公开视频优先交给 yt-dlp；单图帖明确返回“没有视频”时，会读取同一次匿名公开响应中的最佳图片候选；轮播中一部分子项缺少视频格式时，会从同一次 yt-dlp JSON 中保留其余可用子视频，并以带原帖 Referer 的临时公开 CDN 地址下载。签名 URL 可能过期，需重新解析。新版 `data-sjs` 结构按 shortcode 对应的数字媒体 ID 匹配当前作品。私密账号、Stories、Close Friends、年龄限制、匿名频率限制以及必须登录后才能打开的帖子不支持，也不会导入 Instagram Cookie。
- Facebook 当前支持公开 Reel、Watch、视频帖、`fb.watch` 与可由官方跳转展开的分享链接。Facebook 分享页对未登录请求返回 HTTP 400、登录页或“only available for registered users”时会统一显示访问限制，不再误报成普通网络故障；不会使用 Facebook 登录态、访问令牌或私有 Graph API。普通相册和多图帖子目前只能取得页面明确公开的当前 Open Graph 图片，不能保证完整图集。
- YouTube 当前支持单个公开视频、`youtu.be`、Shorts、Live 和嵌入链接，并在匿名视频流可用时保留多清晰度与独立音频选择；`--no-playlist` 会阻止整张播放列表被批量解析。YouTube 可能按网络出口要求登录验证、Cookie 或 PO Token；拾帧不收集这些凭据。诊断链接在同一出口使用 `default`、`android_vr`、`web_embedded`、`web_safari` 和 `tv` 均被要求确认不是机器人，说明更换播放器客户端不能修复该出口。ReClip 也是在另一台电脑/服务器上调用同一个 yt-dlp，只有其服务器网络没有被 YouTube 限制时才可能表现不同，不是 Android 本地绕过方案。
- 豆包支持匿名公开 `/thread/` 页面多层编码的原图字段，以及带公开查询参数的 `/video-sharing` 页面和其官方匿名信息、播放信息路由。`video-sharing` 的匿名接口目前可能只返回 URL 中明确标有 `video_gen_watermark` 的预览视频；拾帧会展示封面、分辨率和红色水印提示，并允许用户自行预览或下载，保存文件会原样保留水印。私有 `/chat/` 会话、区域/账号限制、缺失分享参数和只在登录后返回的生成结果仍不支持；Android 版不会接管登录态、导入 Cookie、解密私有响应或修改视频像素。
- 小红书和快手解析器只读取公开网页内当前作品的结构化状态。小红书新页面公开原图 CDN 基址和 `fileId` 时会请求对应原始图片，而不是带平台展示层的 H5 缩略图；如果这些字段缺失，会退回页面公开展示图并显示水印状态未验证。小红书的 `noteData` / `noteDetailMap`、快手的 `INIT_STATE` / `photo.manifest` 若被平台移除、加密或改为登录后下发，需要随平台规则更新；遇到验证码时会按访问限制处理，不尝试规避。
- 网易云音乐专用解析当前只处理 `163cn.tv` 或 `music.163.com` 的公开单曲链接。歌曲时长优先来自 `music:duration`，缺失时读取官方移动页当前 `Song` 的 `REDUX_STATE.dt`；MP3 头可解析且公开文件明显只有 30–90 秒时标成“仅试听片段”。页面同时缺少两种时长、MP3 使用无法估算的编码或开头元数据超过探测范围时，应用无法仅凭 HTTP 响应绝对确认完整性；不会为此绕过会员、付费、地区或登录限制。
- QQ 音乐当前正式支持公开单曲链接和可展开到单曲 `songmid` 的短链；专辑、歌单、歌手和排行榜即使被 yt-dlp 识别，也会由 `--no-playlist` 阻止批量下载。免费公开单曲可显示多档完整音质；会员、数字专辑或付费歌曲如果官方页面只公开试听，应用仅保存该公开片段并明确标注“仅试听片段”，不会伪装成完整歌曲，也不会导入 QQ 登录 Cookie。
- 酷狗、酷我和咪咕当前没有可靠、持续可用的 yt-dlp 正式提取器。酷狗会读取官方单曲分享页内的 `phpParam.song_info.data`；明确的 `audition_status` / `pay_type` 试听候选只作为“仅试听片段”展示和下载。酷狗 MP3 常被 CDN 标成 `application/octet-stream`，应用会额外验证官方域名、音频扩展名和文件头；临时 URL 过期后仍需重新解析。酷我和咪咕页面 URL 中明确带试听标记的候选采用相同声明。
- 音乐平台临时音频地址可能过期。QQ 音乐由 yt-dlp 返回的 48/96/128 kbps 等条目是不同码率或编码的音质版本，并非视频“清晰度”；可用档位由该歌曲的公开授权决定。QQ 下载会先尝试解析时已验证的官方 CDN 地址，失效后再自动回退 yt-dlp 重新取源；网易云音频会在真正下载前刷新地址。QQ 页面兜底音频以及酷狗、酷我、咪咕的页面直链目前仍可能需要重新解析才能刷新。
- 已知平台的清晰度选项会在下载时由 yt-dlp 重新选择对应视频流，并在需要时用 FFmpeg 合并音频；合并不重新编码画面。极少数平台若没有兼容音轨，界面会明确显示“仅视频”。
- 拾帧不做图像修复、裁切或算法擦除。它会检查提取器返回的公开格式：若存在 `play_addr` / `Direct video` 等公开原始播放源，只展示这些源；明确标记为 `watermarked` 的格式不会作为下载项。
- “公开原始源”表示平台/提取器明确标记的原始播放地址；“公开无水印播放源”表示已排除平台带水印入口，但仍可能经过平台转码。作者上传前自行加入的文字、Logo 或水印仍属于作品画面，应用无法也不会自动移除。
- 小红书原始图片可能仍包含作者在上传前写入的摄影署名、签名或其他创作标识；拾帧只避开平台下载展示层，不擦除、裁剪或修复这些作者标识。
- 为避免“只剩音频”和过度解析失败：水印敏感平台存在公开原始源时优先且只使用原始源；没有原始标记时会保留状态未知、但未被明确标记为水印的候选，并在界面警告无法保证；只有全部视频格式都明确标记为 `watermarked` 时才停止下载。拾帧不通过登录 Cookie、私有接口、签名伪造或访问控制绕过来获取其他版本。
- X/Twitter 动画 GIF 的公开媒体源通常是 MP4。应用会在下载完成后本地转换为 GIF；GIF 只有 256 色，文件通常比 MP4 更大，视觉质量也可能低于平台的 MP4 源。
- X/Twitter 的视频和动画 GIF 优先由 yt-dlp 按当前 `status` ID 解析；纯图片只使用该状态页公开的 `og:image` / `twitter:image`，并请求公开的 `name=orig`。需要登录、页面未暴露结构化图片或提取规则过期时仍会失败，但不会退化成扫描头像、推荐帖和其他回复。
- 抖音图文帖会读取公开页面内的 `_ROUTER_DATA`，只使用 `images.url_list` 并排除明确带 `water` 的 `download_url_list`。页面目前公开的是保持原宽高的 q80 JPEG/WebP；这代表页面最高公开尺寸，不等同于作者上传前的无损原文件。
- 抖音公开视频页若只给出 `/playwm/`，本版本会用同一公开视频 ID 请求 `/play/` 的 720p 公开播放源，并在下载视频或本地提取音频时携带解析得到的最终作品页 Referer。实测问题链接的两者均为 1280×720，前者有平台水印、后者无平台水印。页面中可能另有 4K 等作品尺寸字段，但如果没有对应公开文件，应用不会将 720p 下载项标成 4K；播放地址过期或平台关闭该公开入口时仍会解析或下载失败。
- 已知视觉平台的通用网页兜底若只发现背景音乐，会判定为解析失败，不再把单个 MP3 冒充完整的图文或视频结果。
- 内嵌预览使用平台返回的临时媒体流，可能因签名过期、Referer/地区限制或手机解码器能力而无法播放。预览失败不等同于后台下载必然失败。
- 不创建、不调用来源不明的远程解析服务；全部解析在设备端发起。

## 下载与存储

- Android 10+ 下载按 MIME 类型写入系统 MediaStore：视频到 `Movies/MediaExtractorDemo`，图片/GIF 到 `Pictures/MediaExtractorDemo`，音频到 `Music/MediaExtractorDemo`，其他文件到 `Download/MediaExtractorDemo`；Android 8/9 写入应用专属外部目录，卸载应用时系统可能删除该目录中的文件。
- 服务器不提供 `Content-Length` 时，下载前无法精确预估文件大小；任务仍会在写入错误时安全失败并记录原因。
- 某些媒体 URL 带短期签名，长时间等待后可能过期，需要重新解析。
- 独立“下载音频”只在解析器能确认视频含有音轨时显示。已知平台优先直接取得最佳公开音轨；只有视频直链时需要先把视频下载到应用缓存，再在手机本地提取为 M4A，因此临时占用空间和网络流量可能接近完整视频。原音轨无法封装为 M4A 时会转为 192 kbps AAC。
- 已知平台的 HLS/DASH 由 yt-dlp 与 FFmpeg 处理；通用网页解析器仍最适合直接文件 URL。未知站点只公开一个分片清单时可能无法正确下载。

## 本地转换

- APK 目前只构建 `arm64-v8a`。不能安装到纯 32 位设备，也不能直接在常见 x86/x86_64 模拟器运行。
- 本版本使用 FFmpeg 8.0.1 的 arm64 静态可执行文件；ELF 检查没有 `libexpat`、`libfontconfig` 或 `libc++_shared` 动态依赖，已消除 0.1.1 日志中的 `libexpat.so.1 not found` 链接失败。由于当前电脑未连接真机，仍需在目标手机上执行一次实际转换来完成运行时验收。
- 转换是前台页面内的本地任务。旋转/普通界面重组会保留 ViewModel，但强制停止或系统杀死进程后不会从中间续转；再次启动时 Room 会把遗留的“进行中”转换标记为失败，避免假装仍在执行。
- WorkManager 下载可以在进程重建后恢复；转换任务暂未迁移到 WorkManager。
- GIF 无法读取总时长时，进度条可能在准备状态停留较久并在结束时直接到 100%，但任务状态与取消仍有效。
- 视频转 GIF 使用调色板滤镜，输出可能很大；本版本在开始前要求缓存至少有 100 MB 空间，但不能预知所有输出的最终体积。
- GIF 转 MP4 使用 H.264 `libx264` 和 `yuv420p`，宽高会向下调整为偶数。透明背景不能在普通 H.264 MP4 中保留。
- 若调色板滤镜或 `libx264` 在某台设备上失败，转换器会自动重试简化 GIF 滤镜或 MPEG-4 Part 2 编码；兼容结果可能比首选编码文件更大。
- 图片批量写入 SAF 目录不是事务操作；如果中途失败，已经写入的前几张图片不会自动删除。

## 验证范围

- 本机已完成 JVM 单元测试、KSP/Room 代码生成、Debug APK 编译、签名和 APK 内容检查。
- 当前开发环境没有连接 Android 真机，因此没有在本机实际启动 arm64 FFmpeg 二进制、安装 APK 或验证各厂商系统文件管理器行为。相关步骤已列入 [TESTING.md](TESTING.md) 的真机验收清单，不会被表述为已验证。

## 界面与图标

- 当前 Launcher Icon 使用原照片的确定性居中裁切和围巾棕安全边，没有进行生成式重绘或透明背景主体分割，因此能保持猫咪、枫叶和围巾与用户提供素材一致。不同厂商会套用圆形、圆角方形或其他 Adaptive Icon 蒙版，最终视觉仍需在目标手机桌面检查。
- 如需进一步做专业透明前景抠图，建议交付 1024×1024 透明 PNG，猫脸、头顶枫叶和围巾识别区域放在中央约 672×672 px 内。替换 `drawable-nodpi/app_icon_foreground.png` 即可，不应把该素材用于任何 Compose 页面。
- 本机没有可运行当前 arm64-only APK 的 Android 模拟器或已连接真机，因此浅色、深色、最大系统字体和各厂商小屏的最终像素级外观仍需要按 [TESTING.md](TESTING.md) 的 UI 清单进行真机验收；本机只完成编译、Lint 与静态布局检查。

## 诊断报告

- 诊断报告用于定位失败阶段，不会自动修复平台接口、已过期签名 URL、地区限制、登录限制或设备缺少的编码能力。
- 为避免泄露凭据，报告会删除 URL 查询参数、fragment、Cookie、Token、Authorization 和 content URI 细节；因此依赖签名查询参数的服务只能判断“签名 URL 相关失败”，不能从报告中还原原始签名。
- 日志采用两份各 512 KiB 的应用私有轮转文件，长期运行后的最早事件会被覆盖。复现问题后应尽快导出。

## 发布提醒

- 拾帧目前是个人学习和测试项目，不是 Google Play 上架方案。媒体下载能力可能受到平台服务条款和应用商店政策限制；发布前必须另行做法律、政策、隐私和许可证审核。
