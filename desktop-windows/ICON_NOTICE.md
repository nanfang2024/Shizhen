# Windows 图标素材说明

- 原始文件：`Shizhen.Windows/Assets/app-icon-source.jpg`（默认被 `.gitignore` 排除）。
- 生成文件：`Shizhen.Windows/Assets/Shizhen.ico`，包含 16、24、32、48、64、128 和 256 px 图层。
- 使用位置：项目 `ApplicationIcon`、`MainWindow.Icon`、Windows 标题栏、EXE、桌面和开始菜单快捷方式。
- 未使用位置：品牌开屏、链接页、转换页、历史页、空状态、错误状态、成功状态和任何普通 XAML `Image`。

该图片由用户提供，其版权和分发许可没有随代码一并证明。图片与 ICO 衍生文件不自动适用仓库的 GPL-3.0 许可。公开发布前，发布者必须确认拥有复制、修改和分发该图片的权利；否则应替换为自行创作或具有明确再分发许可的 1024×1024 方形 PNG，并重新生成 ICO。
