<UserControl x:Class="Shizhen.Windows.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Shizhen.Windows.ViewModels">
    <ScrollViewer VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Disabled"
                  HorizontalContentAlignment="Stretch" Padding="0,0,8,0">
        <Grid MaxWidth="1040" Margin="32,28,32,40" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="任务历史" Style="{StaticResource PageTitleStyle}" />
                    <TextBlock Text="解析、下载与转换记录保存在本机" Style="{StaticResource PageSubtitleStyle}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="刷新" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding RefreshCommand}" />
                    <Button Content="清空全部" Style="{StaticResource DangerButtonStyle}" Command="{Binding ClearCommand}" Margin="8,0,0,0" />
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource NoticeCardStyle}" Margin="0,20,0,0">
                <Grid>
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="打开日志目录" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenLogFolderCommand}" />
                        <Button Content="导出诊断文件" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding ExportDiagnosticsCommand}" Margin="8,0,0,0" />
                    </StackPanel>
                </Grid>
            </Border>

            <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="0,16,0,0" Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibility}}">
                <StackPanel HorizontalAlignment="Center" Margin="0,42">
                    <Border Width="60" Height="60" CornerRadius="18" Background="{DynamicResource PrimaryContainerBrush}">
                        <Viewbox Margin="16">
                            <Path Data="M3,4 L3,8 L7,8 M3.5,7.5 A7,7 0 1 0 5,3 M10,5 L10,10 L13,12" Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="1.5" Fill="Transparent" StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                        </Viewbox>
                    </Border>
                    <TextBlock Text="还没有历史记录" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,14,0,0" />
                    <TextBlock Text="完成一次解析、下载或转换后会显示在这里" Style="{StaticResource CaptionStyle}" HorizontalAlignment="Center" Margin="0,5,0,0" />
                </StackPanel>
            </Border>

            <ItemsControl Grid.Row="3" ItemsSource="{Binding Items}" Margin="0,6,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:HistoryItemViewModel}">
                        <Border Style="{StaticResource CardStyle}" Margin="0,10,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding OperationType}" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center" />
                                        <Border Background="{DynamicResource SurfaceSecondaryBrush}" CornerRadius="7" Padding="8,3" Margin="10,0,0,0">
                                            <TextBlock Text="{Binding StatusDisplay}" Foreground="{Binding Record.Status, Converter={StaticResource StatusBrush}}" FontSize="12" FontWeight="SemiBold" />
                                        </Border>
                                        <TextBlock Text="{Binding TimeDisplay}" Style="{StaticResource CaptionStyle}" VerticalAlignment="Center" Margin="10,0,0,0" />
                                    </StackPanel>
                                    <TextBlock Text="{Binding Source}" Foreground="{DynamicResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" MaxWidth="720" Margin="0,7,0,0" />
                                    <TextBlock Text="{Binding OutputPath, StringFormat=输出：{0}}" TextTrimming="CharacterEllipsis" MaxWidth="720" Margin="0,5,0,0" Visibility="{Binding OutputPath, Converter={StaticResource StringToVisibility}}" />
                                    <TextBlock Text="{Binding ErrorReason}" Foreground="{DynamicResource ErrorBrush}" Margin="0,5,0,0" Visibility="{Binding HasError, Converter={StaticResource BoolToVisibility}}" />
                                    <Expander Header="查看详情" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,7,0,0">
                                        <TextBlock Text="{Binding Details, TargetNullValue=没有更多技术详情。}" Style="{StaticResource CaptionStyle}" TextWrapping="Wrap" Margin="0,7,0,0" />
                                    </Expander>
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top" Margin="16,0,0,0">
                                    <Button Content="打开输出" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding DataContext.OpenCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" IsEnabled="{Binding HasOutput}" />
                                    <Button Content="删除" Style="{StaticResource DangerButtonStyle}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="8,0,0,0" />
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
    </ScrollViewer>
</UserControl>
